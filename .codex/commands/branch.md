Deprecated alias command.

Use:

```text
/new-branch <type> <short-description> [working_tree=block|pass|stash] [push=off|on]
```

`/branch` remains available only as compatibility alias. New usage should be `/new-branch`.
